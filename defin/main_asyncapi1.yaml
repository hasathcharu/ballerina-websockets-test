asyncapi: 2.5.0
info:
  title: /
  version: 0.1.0
servers:
  development:
    url: "{server}:{port}/"
    protocol: ws
    protocolVersion: "13"
    variables:
      server:
        default: ws://localhost
      port:
        default: "70"
channels:
  /:
    description: Allows clients to get real-time data on users.
components: {}
x-dispatcherKey: event
